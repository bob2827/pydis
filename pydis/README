Pydis is a transparent redis binding for basic python types It acts as a thin
wrapper around redis-py functinos toprovides list-like and dict-like types that
directly access corresponding redis types.

For example:
l = pydis.list(
